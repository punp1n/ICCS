# Etapa 2: embeddings + rerank (CNP -> ICCS)
# Modelos: Qwen/Qwen3-Embedding-0.6B (embeddings) y BAAI/bge-reranker-v2-m3 (rerank).
# En CPU instalar torch desde el índice CPU:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.4
sentence-transformers>=3.3      # soporte nativo Qwen3-Embedding y CrossEncoder
transformers>=4.51              # arquitectura Qwen3
accelerate>=0.30
pandas>=2.0
pyarrow>=15.0
openpyxl>=3.1
scikit-learn>=1.3
numpy>=1.26
tqdm>=4.65
